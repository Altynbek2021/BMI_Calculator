import 'package:flutter/material.dart';

const bottomContainerHeight = 80.0;
const activeBlankColor = Color.fromARGB(255, 82, 90, 131);
const inactiveBlankColor = Color.fromARGB(255, 33, 40, 73);
const klableTextstyle = TextStyle(color: Colors.black, fontSize: 29);
const knumLblTextstyle =
    TextStyle(color: Colors.black, fontSize: 50.0, fontWeight: FontWeight.w700);
